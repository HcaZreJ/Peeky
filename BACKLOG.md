# BACKLOG

（当前无 in-progress work-unit。两大渲染重构均已交付并验收：markdown 渲染重构（WKWebView + 内嵌真 github-markdown-css，状态见 `.claude/plans/markdown-render-redesign.md`）；JSON/JSONL 渲染简化 + 集中式主题源（pretty-print 文本 + `JSONHighlighter` + `PeekyTheme`，状态见 `.claude/plans/2026-07-20-json-jsonl-render-simplify.md`，前序批次见 `.claude/plans/2026-07-03-peek-native.md`）。后续跟踪：代码高亮 issue #16、原生死路径清理 issue #17、GLFM issue #14；JSON/JSONL follow-up（raw 模式主题统一、全 app color theme 统一、大文件明暗切换 benchmark、tokenize range 参数精简）记于 JSON/JSONL plan 的 Follow-ups 节，细化后另批。）
